## Remove Blog Section

1. Delete `src/components/BlogSection.tsx`.
2. In `src/pages/Index.tsx`, remove the `BlogSection` import and its `<BlogSection />` usage.
3. In `src/components/Navbar.tsx`, remove the "Blog" nav item (`/#blog`).